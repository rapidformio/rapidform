{
  "private": true,
  "name": "app",
  "version": "0.0.0",
  "scripts": {
    "build": "nuxt generate",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "lint": "eslint --ext .ts,.js,.vue . --fix",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf .output"
  },
  "devDependencies": {
    "dotenv": "^16.0.1",
    "eslint": "^8.17.0",
    "eslint-config-custom": "*",
    "nuxt": "3.0.0-rc.3",
    "tsconfig": "*",
    "ui": "*"
  }
}
